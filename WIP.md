# WIP — Work In Progress

## Current Status

- **Game data unpacking** — extracts XML/JS/CSS/HTML files from X4 `.cat`/`.dat` archives for the base game and all DLCs
- **Save file cleaning** — strips full save files down to just the `<universe>` element for faster loading
- **Universe hex map** — interactive hex grid showing all sectors, coloured by controlling faction
- **Sector drill-down** — click a sector to open a 3D scatter plot of all components (stations, ships, gates, data vaults, highways)
- **Component info panel** — displays name, owner faction, station type, and 3D coordinates for a selected component
- **Faction colour coding** — all major factions mapped to distinct colours (Argon, Paranid, Teladi, Split, Terran, Boron, Xenon, Kha'ak, player, etc.)
- **Multi-sector support** — clusters with multiple sectors are rendered as sub-hexes within the parent hex cell


## In Progress


## To Do

### Game data unpacking
- [ ] Add progress output to `unpack-game-data.py` (file count / bytes extracted) so the user knows it's working on large archives
- [ ] Fix unclosed file handle — `inf_data` is opened but never explicitly closed in `unpack-game-data.py`
- [ ] Add validation that the `.dat` file exists alongside the `.cat` before trying to open it
- [ ] `extract.bat` has hardcoded absolute paths — make them relative so the repo works from any location

### Save file cleaning
- [ ] `extract-save-file.bat` has hardcoded absolute paths — make them relative
- [ ] Add pretty-print / indented output option to `clean-save-file.py` so the resulting XML is easier to inspect
- [ ] Add a check that the source file exists and is a valid XML file before processing

### Universe hex map
- [ ] Sector name labels are set to `font-size: 2px` — they are effectively invisible; increase to a readable size
- [ ] Add pan and zoom to the universe map (currently only the sector scatter plot supports this)
- [ ] Add a faction colour legend so the map is self-explanatory
- [ ] Add a hover tooltip showing sector name, faction, and any flags (data vault, ownerless ship, enemy presence)
- [ ] The save file loaded is hardcoded to `save_data\\save_008_universe.xml` in `index.html` — add a file picker or dropdown to choose which save to load
- [ ] The sector position data (`sortedSectorData`) is a large hardcoded array in `x4_save_file_analysis.js` — derive this from the extracted game data files instead

### Sector drill-down (3D scatter plot)
- [ ] Shift+scroll to zoom is non-obvious — add an on-screen hint or change to plain scroll
- [ ] Add a reset button to return the 3D view to its default orientation
- [ ] Add toggles to show/hide component types (stations, ships, gates, data vaults, highways)
- [ ] Ships are filtered to only player, Kha'ak, Yaki, Xenon, and ownerless — consider making this filter configurable or showing all ships
- [ ] The scatter plot re-renders on every mouse event; consider debouncing for smoother performance

### Component info panel
- [ ] Only shows on click — add a hover tooltip directly on the scatter plot points
- [ ] Show richer detail: ship class size, station module count, full macro name

### Faction colour coding
- [ ] Several hostile factions (Kha'ak, Xenon, Yaki, criminal, smuggler, scaleplate, buccaneers) all share the same red — differentiate them with distinct shades
- [ ] Add a legend to the sector scatter plot view as well as the universe map

### General / Infrastructure
- [ ] `initSectorData` is called twice in `index.html` — the second call (for `zones.xml`) should call `initZoneData` instead; gate positions may not be loading correctly
- [ ] `zoneFilter` and `stationFilter` arrays in `x4_save_file_analysis.js` appear unused — remove or wire them up to actual UI filters
- [ ] Several commented-out bar/list chart blocks remain in `index.html` — clean up or restore them
- [ ] The D3 v4 callback-style `d3.xml()` calls are deeply nested — refactor with promises or `async/await` to improve readability and error handling
- [ ] Clarify where the `map_data/` XML files come from and whether they should be generated by the unpack step or committed separately


## Done

- Updated README with full project summary, script descriptions, data directory layout, and getting started guide
